I'm going to describe our current Salesforce CI/CD process (CloudBees Jenkins + sfdx + GitHub, pro-code developers only). Summarize it into a short, structured overview a senior engineering audience can absorb in under 2 minutes.
Structure the output as:

Flow (one line per stage): developer commit → PR → build/validate → deploy, showing which tool does what at each stage
Environments: the org/branch mapping and promotion path (e.g. feature → int → UAT → prod)
Quality gates: what's automated (PMD/static analysis, test runs, code coverage, approvals) and where they sit
Manual steps: anything a human does by hand today
Known gaps/pain points: max 5 bullets

Keep it under 300 words. No marketing language. If anything I described is ambiguous or missing (e.g. rollback process, hotfix path, how destructive changes are handled), list those as open questions at the end instead of guessing.
